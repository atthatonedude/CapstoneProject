<Window x:Class="CapstoneProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CapstoneProject.MVVMBase"
        xmlns:vm2="clr-namespace:CapstoneProject.ViewModel"
        xmlns:local="clr-namespace:CapstoneProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.DataContext>
        <vm2:NavigationViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="userLogin" Content="Login" Command="{Binding LoginCommand}"/>
        <Grid Grid.Row="1">
            <ContentControl x:Name="ViewWindow" Content="{Binding View}"/>
        </Grid>
    </Grid>
</Window>
